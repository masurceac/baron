export const RESPONSE_SCHEMA_PROMPT = `Provide the response as a valid JSON object that strictly adheres to the following JSON schema.
Do not output any text, markdown, or code outside of the single JSON object itself.
Schema: {{schema}}`;

import { ReactNode } from 'react';

export function DetailsGroup(props: { children: ReactNode }) {
  return <div>{props.children}</div>;
}

export function DetailsItem(props: { children: ReactNode }) {
  return <div>{props.children}</div>;
}

export function DetailsLabel(props: { children: ReactNode }) {
  return <div>{props.children}</div>;
}

export function DetailsValue(props: { children: ReactNode }) {
  return <div>{props.children}</div>;
}
